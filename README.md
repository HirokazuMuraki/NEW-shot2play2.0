# NEW-shot2play2.0
NEW-shot2play is a cross-device authentication and entitlement protocol that separates identity verification from authorization by treating entitlements as protocol-managed objects evaluated through deterministic policy rules.
